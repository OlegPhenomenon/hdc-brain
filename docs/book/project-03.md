# Проект III. Мини-кодбук для русских слов

---

## Что строим

Обучаемый кодбук (codebook) — таблицу, которая превращает токены в HDC-вектора. Это центральный компонент HDC-Brain: без кодбука модель не может "видеть" слова.

В проекте II мы использовали случайные вектора для слов. Здесь — учимся делать вектора осмысленными: похожие слова должны получать похожие вектора.

---

## Что тебе нужно знать

- **BPE-токенизация** — глава 9, секция "Как работает BPE: пошагово". Нужно превратить текст в токены прежде чем строить кодбук.
- **Позиционное кодирование** — глава 10, секция целиком. Кодбук хранит "смысл" токена, а позиция добавляется отдельно через Permute.
- **Codebook в HDC-Brain** — глава 11, секция "Codebook — словарь как отпечатки пальцев". Это архитектурная основа того что мы строим.
- **Bind и Bundle** — глава 6. Используем для обучения кодбука через контекст.
- **STE (Straight-Through Estimator)** — глава 8, секция целиком. Нужен чтобы обучать bipolar вектора градиентами.

---

## Спецификация

### Три функции

```python
import torch
import torch.nn as nn

class MiniCodebook(nn.Module):
    """
    Обучаемый кодбук: vocab_size токенов → bipolar вектора размерности dim.

    Внутри:
    - nn.Embedding(vocab_size, dim) — обучаемые float-вектора
    - sign() + STE — бинаризация при forward pass
    """

    def __init__(self, vocab_size: int, dim: int = 1024):
        super().__init__()
        pass

    def forward(self, token_ids: torch.Tensor) -> torch.Tensor:
        """
        token_ids: (batch, seq_len) — индексы токенов
        Возвращает: (batch, seq_len, dim) — bipolar вектора
        """
        pass

    def similarity_matrix(self) -> torch.Tensor:
        """
        Возвращает матрицу (vocab_size, vocab_size) косинусных похожестей
        между всеми парами токенов.
        """
        pass


def train_codebook(texts: list[str], vocab_size: int = 256, dim: int = 1024, epochs: int = 10):
    """
    Обучает кодбук на текстах.

    Алгоритм:
    1. Построй простой токенизатор (символьный или BPE из главы 9)
    2. Для каждого окна [t1, t2, ..., tn] → предскажи tn по [t1..tn-1]
    3. Loss = cosine distance между предсказанием и реальным вектором
    4. Обратное распространение через STE

    Возвращает обученный MiniCodebook.
    """
    pass


def find_similar(codebook: MiniCodebook, token_id: int, top_k: int = 5) -> list:
    """
    Находит top_k токенов, наиболее похожих на данный.
    Возвращает [(token_id, similarity), ...]
    """
    pass
```

---

## Тесты

```python
cb = MiniCodebook(vocab_size=100, dim=512)

# Forward pass возвращает bipolar
ids = torch.tensor([[0, 1, 2, 3]])
out = cb(ids)
assert out.shape == (1, 4, 512)
assert set(out.unique().tolist()).issubset({-1.0, 1.0})

# Матрица похожести — квадратная, диагональ = 1.0
sim = cb.similarity_matrix()
assert sim.shape == (100, 100)
assert torch.allclose(sim.diag(), torch.ones(100), atol=0.01)

# После обучения: похожие слова должны быть ближе
# (этот тест — качественный, запусти и проверь глазами)
```

---

## Улучшения

- Визуализируй матрицу похожести как heatmap (matplotlib). Видны ли кластеры похожих токенов?
- Сравни случайный кодбук vs обученный: выведи top-5 похожих для нескольких слов. Есть ли разница?
- Добавь позиционное кодирование из главы 10 — Permute вектора на позицию. Стала ли генерация лучше?
- Увеличь размерность до 4096. Как меняется качество и скорость?
